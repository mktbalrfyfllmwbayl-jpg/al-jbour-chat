* {
    box-sizing: border-box;
}

body {
    margin: 0;
    font-family: Tahoma, Arial, sans-serif;
    background: #eef2f7;
    color: #222;
}

.app {
    width: 100%;
    max-width: 700px;
    min-height: 100vh;
    margin: auto;
    background: white;
    box-shadow: 0 0 25px rgba(0,0,0,0.08);
    display: flex;
    flex-direction: column;
}

/* الهيدر */

.header {
    background: linear-gradient(135deg, #1261ff, #1748b8);
    color: white;
    padding: 18px;
    display: flex;
    align-items: center;
    gap: 14px;
}

.logo {
    width: 55px;
    height: 55px;
    border-radius: 50%;
    background: white;
    color: #1261ff;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 30px;
    font-weight: bold;
}

.header h1 {
    margin: 0;
    font-size: 23px;
}

.header span {
    font-size: 13px;
    opacity: 0.9;
}

/* الدخول */

.login-page {
    padding: 60px 25px;
    text-align: center;
    flex: 1;
}

.welcome-icon {
    font-size: 65px;
    margin-bottom: 15px;
}

.login-page h2 {
    margin-bottom: 8px;
}

.login-page p {
    color: #777;
}

input {
    width: 100%;
    padding: 15px;
    border: 1px solid #ddd;
    border-radius: 12px;
    font-size: 16px;
    outline: none;
    margin-top: 15px;
}

input:focus {
    border-color: #1261ff;
}

button {
    border: none;
    cursor: pointer;
    border-radius: 12px;
    padding: 13px 20px;
    font-size: 16px;
}

.login-page button {
    width: 100%;
    margin-top: 15px;
    background: #1261ff;
    color: white;
}

.login-page button:hover {
    background: #0d4ed5;
}

/* الدردشة */

.chat-page {
    flex: 1;
    display: flex;
    flex-direction: column;
    min-height: 650px;
}

.hidden {
    display: none !important;
}

.user-bar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 12px 15px;
    border-bottom: 1px solid #eee;
    background: #fafafa;
}

.user-bar button {
    background: #eee;
    color: #333;
    padding: 8px 14px;
    font-size: 13px;
}

/* الرسائل */

.messages {
    flex: 1;
    overflow-y: auto;
    padding: 18px;
    background: #f5f7fa;
}

.message {
    max-width: 80%;
    padding: 11px 14px;
    margin-bottom: 12px;
    border-radius: 16px;
    line-height: 1.5;
}

.message p {
    margin: 3px 0 0;
}

.message.other {
    background: white;
    border: 1px solid #e6e6e6;
    margin-left: auto;
    margin-right: 0;
}

.message.me {
    background: #1261ff;
    color: white;
    margin-right: auto;
    margin-left: 0;
}

/* الصوت */

.voice-box {
    margin: 10px;
    padding: 13px;
    border-radius: 12px;
    background: #f0f4ff;
    color: #1261ff;
    text-align: center;
    font-size: 14px;
}

/* إرسال الرسائل */

.input-area {
    display: flex;
    gap: 8px;
    padding: 12px;
    border-top: 1px solid #eee;
    background: white;
}

.input-area input {
    margin: 0;
}

.input-area button {
    background: #1261ff;
    color: white;
    min-width: 80px;
}

/* الفوتر */

footer {
    text-align: center;
    padding: 15px;
    color: #888;
    font-size: 13px;
    border-top: 1px solid #eee;
}

/* الموبايل */

@media (max-width: 500px) {

    .header h1 {
        font-size: 20px;
    }

    .login-page {
        padding: 45px 20px;
    }

    .chat-page {
        min-height: 600px;
    }

    .message {
        max-width: 88%;
    }

}